#include <synapse/disk/disk_object.hpp>

#include <iostream>
#include <string>

namespace synapse
{
namespace disk
{
    class directory
    {
    public:
        directory(std::string dir_path);

    private:

    }
}
}