#include <synapse/memory/memory.hpp>

namespace synapse {
namespace memory  {

    bool reserve_memory(void* rsv_addr, size_t rsv_size);
}
}

bool synapse::memory::reserve_memory(void* rsv_addr, size_t rsv_size)
{
    
}