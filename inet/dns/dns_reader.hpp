#include <synapse/inet/dns/dns_parse.hpp>
#include <synapse/socket/tcp.hpp>
#include <synapse/socket/udp.hpp>

namespace dns
{
    class reader
    {
    public:
        

    private:
        std::vector<dns_answer> 
    };
}